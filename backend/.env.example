DATABASE_URL=postgresql+asyncpg://tracker:tracker@db:5432/tracker
SECRET_KEY=change-me-to-a-random-secret
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional. Only the AI features need it. Free key from aistudio.google.com.
# Free-tier inputs may be used by Google, so only send test data.
GEMINI_API_KEY=
